<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>io.github.a-n-o-d-e-r</groupId>
		<artifactId>spring-powerpoint</artifactId>
		<version>0.1.0-SNAPSHOT</version>
	</parent>

	<artifactId>spring-powerpoint-spring-boot-autoconfigure</artifactId>

	<name>spring-powerpoint-spring-boot-autoconfigure</name>
	<description>Spring Boot auto-configuration for spring-powerpoint.</description>

	<dependencies>
		<dependency>
			<groupId>io.github.a-n-o-d-e-r</groupId>
			<artifactId>spring-powerpoint-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-autoconfigure</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!--
				Generates META-INF/spring-configuration-metadata.json, so that the powerpoint.* properties
				are documented and completed in the IDE. Declared as a processor path rather than as an
				optional dependency: since JDK 23, processors on the class path are ignored.
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.springframework.boot</groupId>
							<artifactId>spring-boot-configuration-processor</artifactId>
							<version>${spring-boot.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
